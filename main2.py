# self built timer

# users input for hr, min, secs
# learn that your cant have user enter inputs like this so new method
import time


"""
all_times = input('Enter times in HH:MM:SS\n').split()
for time in all_times:
    hour, minute, sec = [int(i) for i in time.split(":")]
    print(hour, "hours", minute, "minutes", sec, "seconds")
"""
